import Link from "next/link";
import type { Challenge } from "@verity/domain";
import { scoreLabel } from "@/lib/challenge-form";
export function ChallengeCard({challenge}:{challenge: Challenge}){return <Link href={`/challenges/${challenge.id}`} className="challenge-card"><div className="card-top"><div className="tag-row">{challenge.tags.map(t=><span key={t} className={t === "Hard" || t === "Expert" ? "tag difficulty" : "tag"}>{t}</span>)}</div><span className="live-dot">Live</span></div><h3>{challenge.title}</h3><p>{challenge.description}</p><div className="card-footer"><div><small>Reward</small><strong>{challenge.reward}</strong></div><div><small>Passing score</small><strong>{scoreLabel(challenge.passingScore, challenge.maxScore, challenge.scoreUnit)}</strong></div><div><small>Capacity</small><strong>{challenge.submissions} / {challenge.maxSubmissions}</strong></div></div></Link>}
