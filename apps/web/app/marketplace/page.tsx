import { SiteHeader } from "@/components/site-header"; import { ChallengeCard } from "@/components/challenge-card"; import { challenges as fixtureChallenges } from "@verity/fixtures"; import type { Challenge } from "@verity/domain";
async function getChallenges(): Promise<Challenge[]> { if (process.env.MOCK_MODE === "true") return fixtureChallenges; try { const res = await fetch(`${process.env.API_PUBLIC_URL ?? "http://localhost:4000"}/api/challenges`, { cache: "no-store" }); if (!res.ok) return []; return (await res.json()).challenges as Challenge[]; } catch { return []; } }
export default async function Marketplace(){const challenges = await getChallenges(); return <main><SiteHeader/><section className="page-head"><span className="eyebrow">Open challenges</span><h1>Good work has a clear bar.</h1><p>Objective tasks with a funded reward pool and a private grader behind every result.</p></section><section className="filters"><button className="selected">All work</button><button>Finance</button><button>Logistics</button><button>Data</button><button>Software</button><button>Hard</button></section><section className="grid">{challenges.map(c=><ChallengeCard key={c.id} challenge={c}/>)}</section></main>}
