export type ChallengeStatus = "live" | "funding" | "closed" | "settled";
export type SubmissionStatus = "UPLOADED" | "QUEUED" | "GRADING" | "GRADED" | "SETTLEMENT_PENDING" | "FINALIZED" | "PAID" | "FAILED" | "UNEVALUABLE" | "GRADER_ERROR" | "TIMEOUT";
export type GradingOutcome = "SCORED" | "UNEVALUABLE" | "GRADER_ERROR" | "GRADING_TIMEOUT";
export type GraderLanguage = "typescript" | "javascript" | "python";
export type ScoreSchema = { passingScore: string; minScore?: string; maxScore?: string; scoreScale: number; scoreUnit?: string };
export type PrivateGrader = { language: GraderLanguage; runtimeVersion: string; entrypoint: string; sourceBase64: string; sourceBytes: number; config: Record<string, unknown>; commitment: string };
export type Challenge = { id: string; title: string; description: string; tags: string[]; reward: string; passingScore?: string; minScore?: string; maxScore?: string; scoreScale?: number; scoreUnit?: string; /** Legacy display-only input; newly persisted challenges use passingScore. */ minimumScore: number; submissions: number; maxSubmissions: number; deadline: string; requester: string; status: ChallengeStatus; context: string; format: string; graderType: string; chainId?: number; contractAddress?: string; graderCommitment?: string };
export type ChallengeRecord = Challenge & { companyId?: string; onchainChallengeId?: string; authorizedBackend: string; grader: PrivateGrader; graderVersion: number; rewardWei: string; passingScore: string; scoreScale: number; passingScoreScaled: string; minScoreScaled?: string; maxScoreScaled?: string; deploymentBlock?: number; createdAt: string; indexed?: boolean; paid?: boolean };
export type Company = { id: string; privySubject: string; createdAt: string; updatedAt: string };
export type Submission = { id: string; challengeId: string; agentWallet: string; payoutAddress: string; objectKey: string; submissionHash: string; status: SubmissionStatus; createdAt: string; scoreScaled?: string; score?: string; /** Deprecated projection compatibility; never used in settlement. */ scoreBasisPoints?: number; outcome?: GradingOutcome; jobId?: string; graderCommitment?: string; graderVersion?: number; settlementNonce?: string; settlementExpiry?: string; settlementSignature?: string; transactionHash?: string; finalizedEventId?: string; failureReason?: string };
export type GradingJob = { id: string; submissionId: string; challengeId: string; submissionHash: string; agentWallet: string; graderCommitment: string; graderVersion: number; attempts: number; status: "queued" | "running" | "complete" | "retry" | "failed"; createdAt: string; claimedAt?: string; grader?: PrivateGrader; solutionObjectKey?: string };
export type WalletNonce = { nonce: string; purpose: "agent_submission"; challengeId: string; expiresAt: string; usedAt?: string };
export type IndexerCheckpoint = { chainId: number; contractAddress: string; blockNumber: number; blockHash: string; updatedAt: string };
export const passes = (scoreScaled: bigint, passingScoreScaled: bigint) => scoreScaled >= passingScoreScaled;
export { sha256Hex, canonicalizeSource, commitment, parseScaledScore } from "./crypto";
